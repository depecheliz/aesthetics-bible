import { PlaceholderScreen } from '../../components/layout/PlaceholderScreen';

export default function PassportScreen() {
  return (
    <PlaceholderScreen
      title="Passport"
      description="Private treatment history, progress photos, providers, calendar, and budget will live here."
    />
  );
}
